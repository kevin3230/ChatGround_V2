package com.chatground.repository;

import com.chatground.entity.MemCRSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemCRSetRepository extends JpaRepository<MemCRSet, Long> {
}
